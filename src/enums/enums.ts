export enum LoggerDescription {
  INSERT = 'Insert item',
  SELECT_ALL = 'Selectd all items',
  SELECT_FILTERS = 'Selectd all items whit filters',
  SELECT_PAGINATION = 'Selectd items paginated',
  SELECT_ONE = 'Selectd one item',
  UPDATE = 'Update item',
  DELETE = 'Delete item'
}
